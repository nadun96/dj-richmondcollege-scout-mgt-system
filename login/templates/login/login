{% extends 'login/base' %}

{% block title %} {{title}} {% endblock %}


{% block content %}

<section class="auth-hero">
    <div class="container">
        <div class="row align-items-center justify-content-between g-5">
            <div class="col-lg-6 order-lg-1 order-2 text-lg-start text-center">
                <span class="gradient-pill">
                    <i class="fa-solid fa-compass"></i>
                    Richmond College Scouts
                </span>
                <h1 class="section-title display-5 mt-4">
                    Lead. Serve. Inspire.
                </h1>
                {% for g in group %}
                <p class="section-lead mt-3">
                    {{ g.about }}
                </p>
                {% endfor %}

                <ul class="feature-list">
                    <li><i class="fa-solid fa-people-group"></i> Unified dashboards for scouts, leaders, and staff</li>
                    <li><i class="fa-solid fa-calendar-check"></i> Seamless event planning and badge tracking</li>
                    <li><i class="fa-solid fa-shield-heart"></i> Secure access with role-based controls</li>
                </ul>
            </div>
            <div class="col-lg-5 order-lg-2 order-1">
                <div class="glass-card p-4 p-lg-5">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h2 class="h3 mb-0 text-white">Welcome back</h2>
                        <span class="badge-soft">Secure Access</span>
                    </div>
                    <p class="text-muted mb-4">Sign in to manage your troop, monitor progress, and stay informed.</p>

                    {% if error %}
                    <div class="alert alert-danger border-0" role="alert">
                        {{ error }}
                    </div>
                    {% endif %}

                    {% if messages %}
                        {% for message in messages %}
                        <div class="alert alert-soft" role="alert">
                            {{ message }}
                        </div>
                        {% endfor %}
                    {% endif %}

                    <form method="post" action="{% url 'home:login' %}" class="needs-validation" novalidate>
                        {% csrf_token %}
                        <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="username" id="username" placeholder="Username" autocomplete="username" required>
                            <label for="username">Username</label>
                        </div>
                        <div class="form-floating mb-4">
                            <input type="password" class="form-control" name="password" id="password" placeholder="Password" autocomplete="current-password" required>
                            <label for="password">Password</label>
                        </div>
                        <button type="submit" class="btn btn-primary w-100 py-2">Sign In</button>
                    </form>

                    <div class="text-center mt-4">
                        <p class="small text-muted mb-2">New to the troop?</p>
                        <a class="btn btn-outline-light w-100" href="{% url 'home:signup' %}">
                            Create an account
                        </a>
                        <p class="small text-muted mt-3 mb-0">Having trouble signing in? Contact your troop administrator for assistance.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

{% endblock %}